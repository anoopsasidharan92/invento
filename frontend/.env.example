# Base URL of the backend API. Leave unset for local development —
# the app falls back to http://localhost:8000.
#
# For the Vercel deployment set this to your Cloudflare Tunnel HTTPS URL.
# The WebSocket URL is derived automatically (https -> wss), so set only this.
# VITE_API_BASE=https://your-tunnel.trycloudflare.com
