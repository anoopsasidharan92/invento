# Copy to `.env` in this folder or use repo-root `.env`.
# Required for any real search results — the agent does not use mock/placeholder pages.
# https://serper.dev
SERPER_API_KEY=

# Optional: mirror deals to Postgres (repo-root .env POSTGRES_URL).
