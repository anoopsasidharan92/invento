# Copy to repo-root .env (same folder as docker-compose.yml):
#   cp postgres.env.example .env
# Matches credentials in docker-compose.yml — local dev only, not for production.

POSTGRES_URL=postgresql+psycopg2://agents:agents_local_dev_only@127.0.0.1:5432/agents
